# /backend — (placeholder)

Réservé au backend node.js (proxy API Claude / lambdas functions), conformément au
Plan de match de l'équipe.

Rôle prévu : servir de **proxy pour l'API Claude** afin de ne pas exposer la clé
API côté navigateur, et accueillir d'éventuelles lambdas functions.

> Le module AST (cœur du projet) reste **côté frontend** dans tous les cas : le
> parsing Babel et la synchronisation des vues tournent dans le navigateur.
